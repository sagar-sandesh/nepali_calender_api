# Nepali Calendar API

A Flask API to provide Nepali (Bikram Sambat) calendar features:

- AD ↔ BS date conversion
- Today's date with Nepali digits and weekdays
- Festivals and important events lookup
- Admin route to add festivals/events

---

## Setup

1. Clone the repo  
2. Create a Python virtual environment (recommended)  
3. Install dependencies:

```bash
pip install -r requirements.txt

